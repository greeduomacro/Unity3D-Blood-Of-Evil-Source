using UnityEngine;
using System.Collections;

public class Door : ADoor {

	//[SerializeField]
	//private float minAngle;
	//[SerializeField]
	//private float maxAngle;


	//void Start () {
	//    base.DoorStart();
	//}
	
	//protected override void Update () {
	//    float angle = Vector3.Angle(transf.forward, transfDoor.forward);

	//    if (open && angle < maxAngle)
	//        transfDoor.RotateAround(transf.position, Vector3.up, openSpeed);
	//    else if (!open && angle > minAngle)
	//        transfDoor.RotateAround(transf.position, Vector3.up, -openSpeed);
	//}
}
