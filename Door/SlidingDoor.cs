using UnityEngine;
using System.Collections;

public class SlidingDoor : ADoor {
    
	//[SerializeField]
	//private float maxTranslate;
	//[SerializeField]
	//private float minTranslate;

	//void Start ()
	//{
	//    base.DoorStart();	
	//}

	//protected override void Update()
	//{
	//    if (open && transfDoor.localPosition.x < maxTranslate)
	//        transfDoor.Translate(transfDoor.right * openSpeed * Time.deltaTime);
	//    else if (!open && transfDoor.localPosition.x > minTranslate)
	//        transfDoor.Translate(-transfDoor.right * openSpeed * Time.deltaTime);
	//}
}
